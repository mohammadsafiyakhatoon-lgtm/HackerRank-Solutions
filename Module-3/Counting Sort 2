n=int(input())
arr=list(map(int,input().split()))
count=[0]*(max(arr)+1)
for num in arr:
    count[num]+=1
for i in range(len(count)):
    while count[i]>0:
        print(i,end=" ")
        count[i]-=1
