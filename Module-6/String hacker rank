A=input()
n=len(A)
max_len=0
for mask in range(1,1<<n):
    sub=""
    for i in range(n):
        if mask & (1<<i):
            sub+=A[i]
    if sub==sub[::-1]:
        if len(sub)>max_len:
            max_len=len(sub)
print(max_len)
